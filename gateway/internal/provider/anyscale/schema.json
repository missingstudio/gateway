{
  "description": "Anyscale configuration schema",
  "title": "Anyscale",
  "type": "object",
  "properties": {
    "auth": {
      "type": "object",
      "title": "Auth",
      "description": "Provider authorization properties",

      "properties": {
        "api_key": {
          "type": "string",
          "title": "API Key",
          "description": "Anyscale API key"
        }
      },
      "required": ["api_key"]
    }
  },
  "required": ["auth"]
}
