{
  "description": "OpenAI configuration schema",
  "title": "Open AI",
  "type": "object",
  "properties": {
    "auth": {
      "type": "object",
      "title": "Auth",
      "description": "Provider authorization properties",

      "properties": {
        "api_key": {
          "type": "string",
          "title": "API Key",
          "description": "OpenAI API key"
        }
      },
      "required": ["api_key"]
    }
  },
  "required": ["auth"]
}
