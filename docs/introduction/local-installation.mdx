---
title: Local
description: How to install and run Missing studio on your developer machine or bare metal setups
---

## Run locally

Missing studio can be deployed in a variety of ways. It is deployable on bare metal, or in dockerized environments.
To start missing studio server, simply run the following command:

```bash
make compose-dev
```
<Info>**Note:** Your AI Gateway is now running on http://localhost:8080 💥.</Info>

## Send a request

After running studio gateway, you can call api using `curl` in your terminal by running:

```bash
curl \
--header "Content-Type: application/json" \
--header "x-ms-provider: openai" \
--header "Authorization: Bearer {{OPENAI_API_KEY}}" \
--data '{"model":"gpt-3.5-turbo","messages":[{"role":"user","content":"who are you?"}]}' \
http://localhost:8080/v1/chat/completions
```
